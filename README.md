Angular Skeleton
================

Clone the repo:

`git clone git@github.com:mbildner/angular-skeleton.git`

Enter the directory:

`cd angular-skeleton/`

Install dependencies:

`npm install`

Start the server:

`node server.js`

Go nuts.
